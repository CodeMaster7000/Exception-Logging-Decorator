# Exception Logging Decorator

## About

A simple exception logging decorator coded in Python. 

NOTE: Logging is a process an app follows to store the process in a log file that could help to analyze/debug.

## Implementation

Run `python3 Exception-Logging-Decorator.py` in a terminal if you have Python 3.
